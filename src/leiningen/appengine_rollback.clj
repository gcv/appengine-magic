(ns leiningen.appengine-rollback
  "Roll the current Google App Engine application to a previous version.")


(defn appengine-rollback [project]
  (println "rolling back project")
  ;; TODO: Fill this in.
  )
